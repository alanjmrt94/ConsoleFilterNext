# Local overrides / plugin versions (también cargados desde ../versions/26.2.properties en build.gradle)
loom_version=1.17.20

# Espejo BMCL: solo vía USE_BMCL_MIRROR=1 / -PuseBmclMirror=1 (ver build.gradle).
